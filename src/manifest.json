{
  "manifest_version": 2,
  "name": "Elodin",
  "version": "1.0.0",
  "description": "Add a keyboard shortcut to generate a random name.",
  "background": {
    "scripts": [
      "browser-polyfill.js",
      "index.js"
    ]
  },
  "permissions": [
    "notifications"
  ],
  "commands": {
    "generate-name": {
      "description": "Add a name to the clipboard",
      "suggested_key": {
        "default": "Ctrl+E"
      }
    }
  }
}
